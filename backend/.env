# Development Environment Configuration
ENVIRONMENT=development
DEBUG=True

# Database Configuration (for local testing - won't actually connect)
DB_HOST=localhost
DB_NAME=memmachine_dev
DB_USER=postgres
DB_PASSWORD=dev_password_123
USE_CLOUD_SQL_CONNECTOR=False

# API Configuration
PORT=8000

# Security (development only - DO NOT use in production)
SECRET_KEY=dev-secret-key-for-testing-only-change-in-production
